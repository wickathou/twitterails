<p id="notice"><%= notice %></p>

<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
      <%= render partial: 'profile' %>
      <% else %>
      <%= render partial: 'trends' %>
      <% end %>
      <%= render partial: 'feed' %>
      <%= render partial: 'who-to-follow' %>
    </div>
  </div>
</section>
